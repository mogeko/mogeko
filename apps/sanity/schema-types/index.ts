import author from "@/schema-types/author";
import blockContent from "@/schema-types/block-content";
import category from "@/schema-types/category";
import post from "@/schema-types/post";

export const schemaTypes = [post, author, category, blockContent];
