---
import Layout, { type HeadProps } from "@/layouts/base-layout.astro";
import SiteHeader from "@/components/site-header.astro";
import SiteFooter from "@/components/site-footer.astro";

const { title, keywords, description } = Astro.props;

type Props = HeadProps;
---

<Layout head={{ title, keywords, description }}>
  <SiteHeader class="container" />
  <article class="container relative m-auto flex min-h-screen flex-col">
    <slot />
  </article>
  <SiteFooter class="container" />
</Layout>
