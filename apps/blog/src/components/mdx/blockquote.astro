---
const { class: className, ...props } = Astro.props;

type Props = astroHTML.JSX.BlockquoteHTMLAttributes;
---

<blockquote
  class:list={[
    "border-primary mb-7 ml-6 mr-7 border-l-[5px] pl-5 italic leading-7 opacity-80",
    className,
  ]}
  {...props}
>
  <slot />
</blockquote>
