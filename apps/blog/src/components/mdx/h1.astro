---
const { class: className, ...props } = Astro.props;

type Props = astroHTML.JSX.HTMLAttributes;
---

<h1
  class:list={[
    "scroll-m-20 text-4xl font-[900] tracking-tight lg:text-5xl [&:not(:first-child)]:mb-7 [&:not(:first-child)]:mt-14",
    className,
  ]}
  {...props}
>
  <slot />
</h1>
